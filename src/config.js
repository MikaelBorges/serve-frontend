// Note : on mets les informations, dans un fichier de config
export let config = {

  // Lorsque l'api est lancée en local :
    // Le pc est connecté en wifi
    // api_url: 'http://localhost:3306',

    // Lorsque le pc est connecté en partage de connexion
    // api_url: 'http://172.20.10.3:3306',

  // Lorsque le pc est éteint est que l'api n'est donc pas lancée
  api_url: 'https://mikaelborges-serve.herokuapp.com',
  cloudname: 'mika4ever'
}
